{
  "name": "codex-cli-gui",
  "version": "1.0.0",
  "description": "An Electron GUI for editing real files, chatting with Codex, and running a real terminal.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "check": "node --check main.js && node --check preload.js && node --check app.js"
  },
  "devDependencies": {
    "electron": "^31.0.0"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/xterm": "^6.0.0",
    "node-pty": "^1.1.0"
  }
}
